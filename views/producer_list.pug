extends layout

block content
    h1= title

    ul
        each producer in producer_list
            li
                a(href=producer.url) #{producer.name}
                |  (#{producer.date_formatted} - #{producer.date_death_formatted})

        else
            li There are no producers.